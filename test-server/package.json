{
  "name": "test-server",
  "version": "1.0.0",
  "description": "Test Node.js on Docker",
  "author": "Ruben Jimenez",
  "main": "tests/*.js",
  "scripts": {
    "test-docker": "docker-compose up test-server",
    "start": "nyc --reporter=html --reporter=text mocha ./tests/*.js",
    "coverage": "nyc report --reporter=text-lcov | coveralls"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "chai": "^4.1.2",
    "chai-http": "^4.2.0",
    "mocha": "^5.2.0",
    "mocha-lcov-reporter": "^1.3.0",
    "istanbul": "^0.4.5",
    "nyc": "^13.1.0",
    "coveralls": "^3.0.2"
  }
}
